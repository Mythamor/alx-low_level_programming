#include <stdio.h>

/**
 * main - print alphabet in lower case, followed by a newline
 * return: 0 (Success)
 */

int main(void)
{

	char c;
	for (c = 'a'; c <= 'z'; c++)

	{
	putchar(c);
	}

	putchar('\n');
	return (0);
}
